# - name: Tap a Homebrew repository using url, state present
#   community.general.homebrew_tap:
#     name: go-task/tap
#     url: https://github.com/go-task/homebrew-tap

- name: Install formulae
  community.general.homebrew:
    name:
      - age # https://github.com/FiloSottile/age
      - fd # https://github.com/sharkdp/fd
      - gh
      - go # https://go.dev/doc/devel/release
      - go-task/tap/go-task # https://taskfile.dev
      - golangci-lint # https://golangci-lint.run/
      - helm
      - hugo
      - just # https://github.com/casey/just
      - kubectx
      - kubernetes-cli
      - nginx
      - optipng # http://optipng.sourceforge.net/
      - terraform
      - terragrunt
      - tflint # https://github.com/terraform-linters/tflint
      - tfsec # https://github.com/aquasecurity/tfsec
      - watchexec # https://github.com/watchexec/watchexec
      - zellij

      # https://github.com/mvdan/sh
      - shfmt

      # https://github.com/koalaman/shellcheck
      - shellcheck
    path: "{{ homebrew_path }}"

# - name: Add formulae to unlink file
#   ansible.builtin.lineinfile:
#     path: "{{ homebrew_unliked }}"
#     line: "{{ item }}"
#   with_items:
#     - kubernetes-cli
