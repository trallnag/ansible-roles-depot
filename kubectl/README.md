# Ansible Role `kubectl`

Role that installs and configures Kubectl.

## Version Management

Set the desired installed and globally activated version via the
`kubectl_version` in [`vars/main.yaml`](vars/main.yaml).

## Cheat Sheet

```sh
asdf latest kubectl
asdf list all kubectl | tail
```
